{% import "Ceres::Basket.Macros.AddToBasket" as AddToBasket %}
{% import "Ceres::Item.Macros.AgeRestriction" as AgeRestriction %}
{% set itemData = config("Ceres.item.data") | split(', ') %}
{% set enabledRoutes = config("IO.routing.enabled_routes") | split(', ') %}

<script type="x/template" id="vue-single-item">
    <div>
      <slot name="default" :getDataField="getDataField" :getFilteredDataField="getFilteredDataField">
        <div id="item-body" class="single container-max page-content">
          <div class="small">{% include "CeresCoconut::PageDesign.Partials.Header.BreadcrumbItem" %}</div>
            <div class="row mt-2 position-relative">
                <div class="col-12 col-md-7" id="item-container">
                    <slot name="image-carousel"></slot>
                </div>

                <div class="mt-2 col-12 col-md-5 single-rightside">
                  <div v-stick-in-parent data-sticky-container="#item-body">
                    {% include "CeresCoconut::Item.Components.SingleItem.SingleItem_Details" %}
                  </div>
                </div>

                <div class="col-12 col-md-7">
                    {% include "CeresCoconut::Item.Components.SingleItem.SingleItem_InformationTable" %}
                </div>
            </div>

        </div>
        <div class="single container-max page-content my-5">
          <slot name="cross-selling-accessory"></slot> 
        <slot name="feedback-container"></slot>
        <div>
      </slot>
    </div>

</script>
